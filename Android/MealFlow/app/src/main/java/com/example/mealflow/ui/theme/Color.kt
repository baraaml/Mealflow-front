package com.example.mealflow.ui.theme

import androidx.compose.ui.graphics.Color

// Primary brand colors - refined green palette
val NiceGreen = Color(0xFF4CAF50)      // More vibrant green with better contrast
val ForestGreen = Color(0xFF2E7D32)    // Slightly brighter forest green for readability
val PastelGreen = Color(0xFFB9F6CA)    // Softer pastel green for secondary actions
val PaleCream = Color(0xFFF5F5F5)      // Neutral light background with subtle warmth
val BrightYellow = Color(0xFFFFB300)   // Warmer, less harsh yellow for accents

// Dark mode palette - refined for better contrast and readability
val DarkGreen = Color(0xFF1B5E20)      // Richer dark green
val MossGreen = Color(0xFF388E3C)      // Brighter moss green for better visibility
val DeepOlive = Color(0xFF33691E)      // Clearer deep olive for distinctness
val DarkBackground = Color(0xFF121212) // Standard dark background for better readability
val GoldenBrown = Color(0xFFFFD54F)    // Brighter golden for dark mode contrast

// Semantic colors
val SuccessGreen = Color(0xFF4CAF50)   // Success states
val ErrorRed = Color(0xFFF44336)       // Error states
val WarningYellow = Color(0xFFFF9800)  // Warning states
val InfoBlue = Color(0xFF2196F3)       // Information states

// Standard system colors
val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)
val TransparentWhite = White.copy(alpha = 0.8f)
val TransparentBlack = Black.copy(alpha = 0.6f)
